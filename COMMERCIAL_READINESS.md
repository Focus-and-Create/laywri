# LayWri 상업적 출시 준비 체크리스트

LayWri는 레이어 기반 글쓰기라는 독창적 콘셉트를 가진 잘 만들어진 프로토타입이다. 하지만 현재 상태(29/100점)로는 상업적 출시가 불가능하다. 아래는 코드베이스 전수 조사를 바탕으로 정리한 영역별 준비사항이다.

---

## 1. 보안 — 현재 점수: 2/10 (치명적)

### 인증/세션
- [ ] 클라이언트 전용 SHA-256 해싱 → **서버사이드 bcrypt/argon2**로 교체
- [ ] `Math.random()` 기반 세션 토큰 → **crypto.getRandomValues()** 또는 서버 발급 JWT로 교체
- [ ] 사용자별 salt 적용 (현재 하드코딩된 `'laywri-salt-v1'` 공용 salt 사용 중)
- [ ] 세션 만료/갱신 로직 서버사이드 이전
- [ ] CSRF 보호 구현

### XSS/입력 검증
- [ ] `innerHTML` 직접 삽입에 **DOMPurify** 등 HTML 새니타이저 적용
- [ ] 붙여넣기 시 스크립트 태그 필터링 (현재 평문 변환만 수행)
- [ ] CSP(Content Security Policy) 헤더 설정
- [ ] 외부 CDN 리소스에 SRI(Subresource Integrity) 해시 추가

### 데이터 보호
- [ ] localStorage 평문 저장 → 암호화 또는 서버 저장소로 이전
- [ ] HTTPS 강제 적용 (HSTS 헤더)
- [ ] 보안 헤더 설정 (X-Frame-Options, X-Content-Type-Options 등)

---

## 2. 데이터 내보내기/백업 — 현재 점수: 0/10 (치명적)

- [ ] **내보내기 기능**: PDF, DOCX, TXT, HTML 중 최소 2가지
- [ ] **가져오기 기능**: TXT, HTML 파일 업로드
- [ ] **전체 백업/복원**: JSON 파일로 전체 메모 일괄 내보내기/가져오기
- [ ] localStorage 용량(~5MB) 모니터링 및 경고 UI
- [ ] 브라우저/기기 간 데이터 이동 수단 제공

---

## 3. 법적 준수사항 — 현재 점수: 0/10 (치명적)

- [ ] **개인정보처리방침** 작성 및 게시
- [ ] **이용약관** 작성 및 게시
- [ ] GDPR 대응: 데이터 삭제권(잊힐 권리), 데이터 이동권(내보내기)
- [ ] 외부 리소스(Google Fonts 등) 사용 고지
- [ ] 쿠키/로컬스토리지 사용 동의 배너 (EU 대상 서비스 시)
- [ ] 계정 탈퇴 및 데이터 완전 삭제 기능

---

## 4. 테스트 — 현재 점수: 0/10 (치명적)

- [ ] 테스트 프레임워크 도입 (Jest, Vitest 등)
- [ ] 핵심 기능 단위 테스트: 레이어 CRUD, normalizeEditor, undo/redo
- [ ] 에디터 통합 테스트: 텍스트 입력, 서식 적용, 레이어 태깅
- [ ] E2E 테스트 (Playwright/Cypress): 메모 생성→편집→저장→재오픈 플로우
- [ ] CI/CD 파이프라인 구성 (GitHub Actions 등)
- [ ] 린팅(ESLint) + 포매팅(Prettier) 설정

---

## 5. 접근성(a11y) — 현재 점수: 2/10 (주요 결함)

- [ ] 에디터에 `role="textbox"`, `aria-multiline="true"`, `aria-label` 추가
- [ ] 모든 버튼/아이콘에 `aria-label` 부여
- [ ] 레이어 토글/저장 등 상태 변경 시 `aria-live` 영역으로 알림
- [ ] 키보드만으로 전체 기능 사용 가능하도록 `tabindex` 및 포커스 관리
- [ ] Skip navigation 링크 추가
- [ ] 색상만으로 레이어 구분하지 않도록 패턴/아이콘 병행
- [ ] WCAG 2.1 AA 기준 색상 대비 검증
- [ ] 포커스 표시(outline) 가시성 확보

---

## 6. 모바일/반응형 — 현재 점수: 3/10 (미흡)

- [ ] **미디어 쿼리 추가**: 최소 3개 브레이크포인트 (모바일/태블릿/데스크톱)
- [ ] 터치 이벤트 지원: 패널 드래그(`touchmove`), 컨텍스트 메뉴(long press)
- [ ] 터치 타겟 최소 44×44px 확보 (현재 버튼 13-14px로 너무 작음)
- [ ] 모바일에서 레이어 패널 → 하단 시트 또는 모달로 전환
- [ ] 하단 고정 툴바가 입력 영역을 가리지 않도록 처리
- [ ] FAB 버튼 위치 모바일 최적화
- [ ] 가상 키보드 올라올 때 레이아웃 대응

---

## 7. 에러 핸들링 — 현재 점수: 3/10 (미흡)

- [ ] `catch(e) {}` 무시 패턴 → 사용자에게 의미 있는 피드백 제공
- [ ] localStorage 용량 초과 시 에러 핸들링 및 안내
- [ ] 손상된 JSON 데이터 복구/폴백 로직
- [ ] `crypto.subtle` 미지원 브라우저 대응
- [ ] 비동기 함수(`hashPassword` 등) try-catch 추가
- [ ] 토스트/스낵바 형태의 사용자 알림 시스템 구축

---

## 8. 성능 — 현재 점수: 6/10 (보통)

- [ ] `normalizeEditor()` 및 `updateLayerStyles()`에 **디바운싱** 적용
- [ ] 대용량 문서(1만 단어+) 대응: 가상 스크롤 또는 청크 렌더링 검토
- [ ] `querySelectorAll()` 결과 캐싱 (반복 호출 방지)
- [ ] 히스토리 스냅샷을 diff 기반으로 전환 검토 (현재 전체 innerHTML 저장)
- [ ] 불필요한 `JSON.parse(JSON.stringify())` 딥클론 최소화

---

## 9. i18n — 현재 점수: 6/10 (보통)

- [ ] 하드코딩된 한국어 제거: 기본 레이어명 `'기본'`, `'대사'`, `'묘사'` → `t()` 함수 사용
- [ ] RTL(아랍어/히브리어) 언어 지원 (글로벌 진출 시)
- [ ] 로케일별 날짜/숫자 포맷 자동 적용
- [ ] 번역 키 누락 시 폴백 로직 강화

---

## 10. 핵심 기술 부채

- [ ] `document.execCommand()` (deprecated) → **ProseMirror, TipTap, Slate** 등 현대적 에디터 엔진으로 교체
- [ ] 서버 백엔드 구축: 인증, 데이터 저장, 동기화
- [ ] 코드 모듈화: 1,900줄 단일 파일 → ES 모듈 분리
- [ ] TypeScript 도입 검토 (타입 안전성)

---

## 우선순위 로드맵

| 단계 | 항목 | 이유 |
|------|------|------|
| **P0 — 즉시** | 보안(XSS 방어, 인증 강화), 데이터 내보내기, 법적 문서 | 사용자 데이터 유실/유출 위험, 법적 리스크 |
| **P1 — 단기** | 테스트 인프라, 에러 핸들링, 모바일 대응 | 안정성과 사용성의 최소 기준 |
| **P2 — 중기** | 접근성, 에디터 엔진 교체, 서버 백엔드 | 규모 확장과 지속 가능성 |
| **P3 — 장기** | 성능 최적화, i18n 확장, 코드 모듈화 | 글로벌 확장, 유지보수성 |

---

## 검증 방법

각 항목 완료 후:
1. **보안**: OWASP ZAP 스캔, 수동 XSS/인젝션 테스트
2. **내보내기**: 각 포맷으로 내보내기 후 재가져오기 데이터 무결성 확인
3. **접근성**: axe DevTools, Lighthouse 접근성 감사 90점 이상
4. **모바일**: Chrome DevTools 기기 에뮬레이션 + 실기기(iOS Safari, Android Chrome) 테스트
5. **테스트**: 커버리지 80% 이상, CI에서 자동 실행
6. **법적**: 법무 검토 완료
